# HW 6 Set Operators

**Name:** Amrina Qayyum  
**Group:** PPG_6  
**Assignment:** HW 6 Set Operators  

## Submitted Files
1. Individual SQL file (Practice + Exercise)
2. MP4 walkthrough recording

## Overview
This assignment focuses on SQL set operators and their use in query construction and data comparison. The work begins with practice based on TSQLV6 examples to understand the logic of set operators. After that, the main exercises are revised and executed in the `Northwinds2024Student` database according to the assignment requirements.

## Set Operators Covered
- `UNION`
- `UNION ALL`
- `INTERSECT`
- `EXCEPT`

## Proposition
In this assignment, the set operator logic is first practiced and tested using TSQLV6-based examples to build understanding of `UNION`, `UNION ALL`, `INTERSECT`, and `EXCEPT`. After that, the main exercises are revised and executed in the `Northwinds2024Student` database, where the goal is to compare business data across tables and time periods in order to identify combined records, common records, and differences.

## Work Completed

### Practice Part
The practice section was completed using TSQLV6-style examples to understand:
- the difference between `UNION` and `UNION ALL`
- how `INTERSECT` finds common rows
- how `EXCEPT` finds rows present in one query but not another

### Main Exercise Part
The main exercise queries were modified to work with the `Northwinds2024Student` database. Adjustments were made based on the actual table and column names in the database.

Completed exercises include:
- **Exercise 1:** Explanation of `UNION` vs `UNION ALL`
- **Exercise 2:** Virtual table of numbers 1 to 10
- **Exercise 3:** Customer-employee pairs in one month but not another
- **Exercise 4:** Customer-employee pairs common in two months
- **Exercise 5:** Customer-employee pairs common in two months but excluded from a previous year
- **Exercise 6:** Combined employee and supplier location data using `UNION ALL`

## Database Used
- Practice / logic testing: **TSQLV6 examples**
- Final revised queries: **Northwinds2024Student**

## Notes
- The original exercise date conditions were revised to match the actual years available in `Northwinds2024Student`.
- The `Orders` table in this database contains data from **2020 to 2022**.
- Employee and supplier location columns were also revised to match the actual schema.

## Final Output
The final submission includes:
- SQL file with practice and revised exercise queries
- Recorded walkthrough presentation in MP4 format

## Author
**Amrina Qayyum**
